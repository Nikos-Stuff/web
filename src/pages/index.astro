---
// export const prerender = false;

import { getCollection } from "astro:content";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard";
// import TeamCard from "@components/TeamCard";
import StackCard from "@components/StackCard.astro";
import { SITE, SOCIALS } from "@consts";
import TwinklingStars from "@components/TwinklingStars.astro";
import MeteorShower from "@components/MeteorShower.astro";
import { Image } from "astro:assets";
import FancyDiv from "@components/FancyDiv.astro";

import Reviews from "@components/Reviews.astro";

import DynamicTitle from "@components/TitleHeader.astro";
import BackgroundFX from "@components/page_effects/BackgroundFX.astro";

import Image1 from "@assets/vid_montage.png";
import Image2 from "@assets/web_dev.png";
import Image3 from "@assets/work_in_progress_panel.png";

const posts = (await getCollection("blog"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => {
    // Sort by date and time
    const dateA = new Date(a.data.date);
    const dateB = new Date(b.data.date);

    const timeA = a.data.time || "00:00";
    const timeB = b.data.time || "00:00";

    const dateTimeA = new Date(`${dateA.toDateString()} ${timeA}`);
    const dateTimeB = new Date(`${dateB.toDateString()} ${timeB}`);

    return dateTimeB.getTime() - dateTimeA.getTime();
  })
  .slice(0, 3);

const projects = (await getCollection("projects"))
  .filter((project) => !project.data.draft)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

// const teammembers = (await getCollection("team"))
//   .filter((teammember) => !teammember.data.draft)
//   .slice(0, 3);

const stack = [
  {
    text: "Lua",
    icon: "lua",
    href: "https://www.lua.org/home.html",
  },
  {
    text: "Astro",
    icon: "astro",
    href: "https://astro.build",
  },
  {
    text: "Javascript",
    icon: "javascript",
    href: "https://www.javascript.com",
  },
  {
    text: "Typescript",
    icon: "typescript",
    href: "https://www.typescriptlang.org",
  },
  {
    text: "PHP",
    icon: "php",
    href: "https://www.php.net",
  },
];

import POD from "@components/POD.astro";

// APOD - Astronomy Picture of the Day

import { NASA_API, AZURE_API } from "astro:env/server";

const apiUrl = `https://api.nasa.gov/planetary/apod?api_key=${NASA_API}`;

let apodData = null;
let translatedExplanation = null;

const azureRegion = "eastus";
const azureEndpoint = `https://api.cognitive.microsofttranslator.com/translate?api-version=3.0&from=en&to=pl`;

try {
  const response = await fetch(apiUrl);
  apodData = await response.json();

  // Prepare the text for translation
  const translationPayload = [
    {
      Text: apodData.explanation,
    },
  ];

  const translateResponse = await fetch(azureEndpoint, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Ocp-Apim-Subscription-Key": AZURE_API,
      "Ocp-Apim-Subscription-Region": azureRegion,
    },
    body: JSON.stringify(translationPayload),
  });

  const translation = await translateResponse.json();
  translatedExplanation =
    translation?.[0]?.translations?.[0]?.text || apodData.explanation;

  console.log(" Translated Explanation:", translatedExplanation);
} catch (error) {
  console.error(" Defaulting to og text - Error:", error);
  translatedExplanation = apodData.explanation;
}
---

<PageLayout title="Home" description={SITE.DESCRIPTION}>
  <!-- <script is:inline data-astro-rerun async src="/js/scroll_animation.js"
  ></script> -->

  <POD />
  <!-- Light Mode: Particles -->
  <div class="absolute inset-0 block dark:hidden">
    <div id="particles1" class="fixed inset-0"></div>
    <div id="particles2" class="fixed inset-0"></div>
    <div id="particles3" class="fixed inset-0"></div>
  </div>

  <!-- Dark Theme: Stars -->
  <div class="absolute inset-0 hidden dark:block">
    <div id="stars1" class="fixed inset-0"></div>
    <div id="stars2" class="fixed inset-0"></div>
    <div id="stars3" class="fixed inset-0"></div>
  </div>

  <!-- Dark Theme: Twinkling Stars / Metors -->
  <div id="galaxy" class="fixed inset-0">
    <div class="hidden dark:block">
      <TwinklingStars />
      <MeteorShower />
    </div>
  </div>

  <!-- <script is:inline defer src="/js/bg.js"></script> -->
  <BackgroundFX />
  <!-- HERO -->
  <section
    id="hero_fade"
    class="snap-center grid w-screen h-svh place-items-center relative text-center overflow-hidden transition-all"
  >
    <!-- <div id="planetcont" class='animate absolute inset-0 top-3/4 overflow-hidden z-10'> PLANET HAS BEEN REMOVED FOR A TEST NEW LAYOUT 
            <div id="crescent"
                 class='absolute top-0 left-1/2 -translate-x-1/2 w-[250vw] min-h-[100vh] aspect-square rounded-full p-[1px] bg-gradient-to-b from-black/25 dark:from-white/75 from-0% to-transparent to-5%'>
                <div id="planet"
                     class='w-full h-full bg-white dark:bg-black rounded-full p-[1px] overflow-hidden flex justify-center'>
                    <div id="blur" class='w-full h-5 rounded-full bg-neutral-900/25 dark:bg-white/25 blur-2xl'/>
                </div>
            </div>
        </div> -->
    <DynamicTitle
      text="Niko's Stuff"
      sub="Składzik Twórczości"
      buttons={true}
      typewriter={true}
    />
  </section>

  <div class="relative">
    <!-- <div class="mx-auto max-w-screen-sm p-5 space-y-24 pb-16">  -->

    <!-- Important Information
    <FancyDiv margin_bottom="50" isAnimated={true} theme="alert">
      <section class="animate pb-0">
        <p class="font-semibold text-black dark:text-white">Informacja</p>
        <p class="font-bold text-sm mb-2">Od dnia 5 sierpnia...</p>

        <div class="relative pl-6">
          <div
            class="absolute top-0 left-2 w-1 bg-black/15 dark:bg-white/20 rounded-full h-full z-0"
          >
          </div>

          <div
            class="absolute top-0 left-2 w-1 bg-black/35 dark:bg-white/40 rounded-full h-[100%]"
          >
          </div>
          <div class="mb-8 flex items-start relative">
            <div>
              <p class="font-semibold">Przygotowania</p>
              <p class="text-sm text-gray-800 dark:text-gray-400 max-w-lg">
                Wszystkie usługi, serwisy itp zostaną przygotowane do migracji.
              </p>
            </div>
          </div>

          <div class="mb-8 flex items-start relative">
            <div>
              <p class="font-semibold">Migracja</p>
              <p class="text-sm text-gray-800 dark:text-gray-400 max-w-lg">
                Stopniowe przenoszenie usług. Możliwe krótkie przerwy w
                działaniu.
              </p>
            </div>
          </div>

          <div class="mb-8 flex items-start relative">
            <div>
              <p class="font-semibold">Finalizacja</p>
              <p class="text-sm text-gray-800 dark:text-gray-400 max-w-lg">
                Finalne testy, usuwanie błędów, stabilizacja na nowym serwerze.
              </p>
            </div>
          </div>

          <div class="flex items-start relative">
            <div>
              <p class="font-semibold">Wydanie</p>
              <p class="text-sm text-gray-800 dark:text-gray-400 max-w-lg">
                Udostępnienie nowej wersji i oficjalne zakończenie procesu.
              </p>
            </div>
          </div>
        </div>
      </section>
    </FancyDiv> -->

    <FancyDiv margin_bottom="50" isAnimated={true}>
      <!--  About  Image Section  -->
      <section class="animate pb-0">
        <p class="font-semibold text-black dark:text-white">
          Obrazek / Informacja Dnia
        </p>
        <!-- <p class="font-bold text-sm mb-2">{apodData.title}</p>
        <p class="text-xs mb-2">{apodData.date} - {apodData.copyright}</p>
        <p class="text-sm max-w-3xl">{translatedExplanation}</p> -->

        <p class="font-bold text-sm mb-2">It's Spooky Month!</p>
        <p class="text-xs mb-2">Glenn Gatsby feat. Ashley Slater - Spooky Scary Skeletons</p>
        <p class="text-sm max-w-3xl">
          Spooky Scary Skeletons przejęły dzisiaj miejsce NASA Picture of the Day! 🎃👻<br />
          Zamiast kosmicznych widoków, czas na odrobinę halloweenowego klimatu i muzyki.<br />
          Uważaj, bo szkieletory mogą zatańczyć na Twoim ekranie! 🦴🕺<br />
          Wesołego (i strasznego) miesiąca!
        </p>
      </section>
    </FancyDiv>

    <FancyDiv margin_bottom="24" isAnimated={true}>
      <section class="animate pb-0">
        <p class="font-semibold text-2xl">Montaż Video</p>
        <p class="font-bold text-sm mb-2">
          Niesamowita jakość w przystępnej cenie.
        </p>

        <div class="animate">
          <Image
            src={Image1}
            alt="Video Editing"
            class="rounded-sm border border-black/25 dark:border-white/25 hover:brightness-150 hover:scale-105 transition-[filter,scale] duration-300"
          />
        </div>
      </section>
    </FancyDiv>

    <FancyDiv margin_bottom="24" isAnimated={true}>
      <section class="animate pb-0">
        <p class="font-semibold text-2xl">Strony WWW</p>
        <p class="font-bold text-sm mb-2">
          Minimalistyczny design, płynne animacje, szybki czas dostawy.
        </p>

        <div class="animate">
          <Image
            src={Image2}
            alt="Web Development"
            class="rounded-sm border border-black/25 dark:border-white/25 hover:brightness-150 hover:scale-105 transition-[filter,scale] duration-300"
          />
        </div>
      </section>
    </FancyDiv>

    <FancyDiv margin_bottom="24" isAnimated={true}>
      <section class="animate pb-0">
        <p class="font-semibold text-2xl">Wysokiej jakości Usługi</p>
        <p class="font-bold text-sm mb-2">
          Intuicyjny panel zarządzania twoich serwerów. <small>(wkrótce)</small>
        </p>
        <div class="animate">
          <Image
            src={Image3}
            alt="Work in progress panel"
            class="rounded-sm border border-black/25 dark:border-white/25 hover:brightness-150 hover:scale-105 transition-[filter,scale] duration-300"
          />
        </div>
      </section>
    </FancyDiv>

    <!--  Review Section -->
    <FancyDiv margin_bottom="50" isAnimated={true}>
      <section class="space-y-4 animate not-prose">
        <p class="font-semibold text-black dark:text-white">Co na to inni</p>
        <Reviews />
        <script is:inline defer src="/js/carousel.js"></script>
        <!-- We use the standard carousel script -->
      </section>
    </FancyDiv>

    <!--  About Section  -->
    <FancyDiv isAnimated={true}>
      <section class="animate pb-0">
        <p>
          Hej! Jestem <span class="rainbow font-semibold">Niko</span>
          <span class="text-xs">(Maciek)</span>. pasjonat programowania z
          zamiłowaniem do edycji wideo, animacji i tworzenia gier. Ukończyłem
          szkołę zawodową i obecnie współtworzę gry oraz cyfrowe doświadczenia w
          małym, zaangażowanym zespole. Nieustannie rozwijam się, eksplorując
          nowe technologie, narzędzia oraz techniki kodowania i projektowania /
          animowania 2D/3D.
        </p>
      </section>
    </FancyDiv>

    <!-- Tech Stack Section -->
    <FancyDiv isAnimated={true}>
      <section class="animate">
        <div class="space-y-4">
          <p class="font-semibold text-black dark:text-white">
            Ucze się / Moje umiejętności
          </p>
          <div class="flex flex-wrap items-center gap-2 mt-5">
            {
              stack.map((item) => (
                <StackCard text={item.text} icon={item.icon} href={item.href} />
              ))
            }
          </div>
        </div>
      </section>
    </FancyDiv>

    <!-- Certificates and stuff -->
    <FancyDiv margin_bottom="50" isAnimated={true}>
      <section class="animate">
        <div class="space-y-4">
          <p class="font-semibold text-black dark:text-white">Osiągnięcia</p>
          <div class="flex flex-wrap items-center gap-2 mt-5">
            <a
              href="https://teb.pl/kierunki/d/technik-programista/nowy-sacz/"
              target="_blank"
              rel="noopener"
            >
              Pracuję nad tym ;)
            </a>
          </div>
        </div>
      </section>
    </FancyDiv>

    <!-- Team Section -->
    <!-- <FancyDiv margin_bottom="50" isAnimated={true}>
      <section class="animate mb-0">
        <div class="space-y-4">
          <div class="flex justify-between">
            <p class="font-semibold text-black dark:text-white">
              Warto o nich wspomnieć
            </p>
            <a
              href="/team"
              class="w-fit col-span-3 group flex gap-1 items-center underline decoration-[.5px] decoration-black/25 dark:decoration-white/50 hover:decoration-black dark:hover:decoration-white text-black dark:text-white underline-offset-2 blend"
            >
              <span
                class="text-black/75 dark:text-white/75 group-hover:text-black group-hover:dark:text-white blend"
              >
                Zobacz wiecej
              </span>
            </a>
          </div>
          <ul class="space-y-4 transition-all duration-300 ease-in-out">
            {
              teammembers.map((teammember) => (
                <li>
                  <TeamCard entry={teammember} />
                </li>
              ))
            }
          </ul>
        </div>
      </section>
       <script is:inline defer data-astro-rerun async src="/js/team_scroll.js"
      ></script> 
    </FancyDiv> -->

    <!-- Blog Preview Section -->
    <FancyDiv margin_bottom="50" isAnimated={true}>
      <section class="animate">
        <div class="space-y-4">
          <div class="flex justify-between">
            <p class="font-semibold text-black dark:text-white">Co Nowego?</p>
            <a
              href="/blog"
              class="w-fit col-span-3 group flex gap-1 items-center underline decoration-[.5px] decoration-black/25 dark:decoration-white/50 hover:decoration-black dark:hover:decoration-white text-black dark:text-white underline-offset-2 blend"
            >
              <span
                class="text-black/75 dark:text-white/75 group-hover:text-black group-hover:dark:text-white blend"
              >
                Wszystkie Posty
              </span>
            </a>
          </div>
          <ul class="space-y-4">
            {
              posts.map((post) => (
                <li>
                  <ArrowCard entry={post} />
                </li>
              ))
            }
          </ul>
        </div>
      </section>
    </FancyDiv>

    <!-- Project Preview Section -->
    <FancyDiv margin_bottom="50" isAnimated={true}>
      <section class="animate">
        <div class="space-y-4">
          <div class="flex justify-between">
            <p class="font-semibold text-black dark:text-white">
              Prosto z pieca
            </p>
            <a
              href="/projects"
              class="w-fit col-span-3 group flex gap-1 items-center underline decoration-[.5px] decoration-black/25 dark:decoration-white/50 hover:decoration-black dark:hover:decoration-white text-black dark:text-white underline-offset-2 blend"
            >
              <span
                class="text-black/75 dark:text-white/75 group-hover:text-black group-hover:dark:text-white blend"
              >
                Zobacz Wszystko
              </span>
            </a>
          </div>
          <ul class="space-y-4">
            {
              projects.map((project) => (
                <li>
                  <ArrowCard entry={project} />
                </li>
              ))
            }
          </ul>
        </div>
      </section>
    </FancyDiv>

    <!-- Contact Section -->
    <FancyDiv margin_bottom="50" isAnimated={true}>
      <section class="animate">
        <div>
          <p class="font-semibold text-black dark:text-white">
            Bądźmy w kontakcie
          </p>
          <p>Dołącz do społeczności, lub zostaw prywatną wiadomość.</p>
          <div class="grid grid-cols-4 gap-y-2 mt-4 auto-cols-min">
            {
              SOCIALS.map((social) => (
                <>
                  <div class="col-span-1 flex items-center gap-1">
                    <span class="whitespace-nowrap truncate">
                      {social.NAME}
                    </span>
                  </div>
                  <div class="col-span-3 truncate">
                    <a
                      href={social.HREF}
                      target="_blank"
                      class="w-fit col-span-3 group flex gap-1 items-center underline decoration-[.5px] decoration-black/25 dark:decoration-white/50 hover:decoration-black dark:hover:decoration-white text-black dark:text-white underline-offset-2 blend"
                    >
                      <span class="text-black/75 dark:text-white/75 group-hover:text-black group-hover:dark:text-white blend">
                        {social.TEXT}
                      </span>
                    </a>
                  </div>
                </>
              ))
            }
          </div>
        </div>
      </section>

      <!-- Line -->
      <hr class="border-black/10 dark:border-white/25 animate" />

      <section class="animate flex justify-center items-center">
        <!--  -->
        <div class="flex justify-between space-x-4 items-center">
          <!-- BETTERSTACK -->
          <a
            target="_blank"
            href="https://uptime.betterstack.com"
            rel="noopener"
          >
            <Image
              src="/betterstack.webp"
              alt="BetterStack."
              width={150}
              height={150}
              loading="lazy"
            />
          </a>

          <!-- IPABUSE -->
          <a
            href="https://www.abuseipdb.com/user/228538"
            title="AbuseIPDB is an IP address blacklist for webmasters and sysadmins to report IP addresses engaging in abusive behavior on their networks"
            class="bg-white/20 dark:bg-white/50 rounded-md p-1"
          >
            <Image
              src="https://www.abuseipdb.com/contributor/228538.svg"
              alt="AbuseIPDB Contributor Badge"
              width={195}
              height={48}
              loading="lazy"
            />
          </a>
        </div>
      </section>
    </FancyDiv>

    <!-- </div> -->
  </div>
</PageLayout>
